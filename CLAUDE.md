@AGENTS.md

## Agent skills

### Issue tracker

Issues and specs live as GitHub Issues on `KindnessofGod/emmason_onliine_store`, via the GitHub MCP tools (no `gh` CLI in this environment). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.

